{
	"name": "open-lovable-sandbox-worker",
	"version": "1.0.0",
	"description": "Cloudflare Worker for sandbox operations using CF Sandbox SDK",
	"main": "src/worker.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@cloudflare/sandbox": "latest"
	},
	"devDependencies": {
		"typescript": "^5",
		"wrangler": "latest"
	}
}
